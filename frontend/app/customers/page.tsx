'use client';
import { useEffect, useState } from 'react';
const API_URL = process.env.NEXT_PUBLIC_API_URL ?? 'http://127.0.0.1:8000';
type Customer = { id: number; name: string | null; churn_probability: number | null; prediction: number | null; created_at: string };
export default function CustomersPage() { const [customers, setCustomers] = useState<Customer[]>([]); const [loading, setLoading] = useState(true); useEffect(() => { fetch(`${API_URL}/customers`).then((r) => r.json()).then(setCustomers).catch(() => setCustomers([])).finally(() => setLoading(false)); }, []); return <main className="mx-auto max-w-[1180px] px-6 py-10 xl:px-10"><p className="text-sm font-semibold text-[#6d8d00]">SAVED MODEL RESULTS</p><h1 className="mt-2 text-4xl font-bold tracking-[-.05em]">Customers</h1><div className="mt-7 overflow-hidden rounded-[24px] border border-slate-200 bg-white"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-slate-500"><tr><th className="px-6 py-4">Customer</th><th className="px-6 py-4">Churn probability</th><th className="px-6 py-4">Risk</th><th className="px-6 py-4">Assessed</th></tr></thead><tbody>{loading ? <tr><td colSpan={4} className="px-6 py-10 text-center text-slate-400">Loading saved assessments…</td></tr> : customers.length ? customers.map((customer) => <tr key={customer.id} className="border-t border-slate-100"><td className="px-6 py-4 font-semibold">{customer.name || `Customer #${customer.id}`}</td><td className="px-6 py-4">{customer.churn_probability === null ? '—' : `${Math.round(customer.churn_probability * 100)}%`}</td><td className="px-6 py-4"><span className={`rounded-full px-3 py-1 text-xs font-bold ${customer.prediction ? 'bg-red-100 text-red-700' : 'bg-lime-100 text-lime-800'}`}>{customer.prediction ? 'High risk' : 'Low risk'}</span></td><td className="px-6 py-4 text-slate-500">{new Date(customer.created_at).toLocaleDateString()}</td></tr>) : <tr><td colSpan={4} className="px-6 py-10 text-center text-slate-400">No customer assessments saved yet.</td></tr>}</tbody></table></div></main>; }
